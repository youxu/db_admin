$(document).ready(function(){
	
	$("#add_project").validate(
			{
				rules:{
					name:{
						required:true,
						remote:{
							url  : "/index.php/ajax/is_same_pro_name",
							type : "POST",
							data : {
									name:function(){return $("#name").val();},
							},
						},
						},
					},
				messages:{
					name:{
							required:"请填写项目名称",
							remote:"项目名称重复",
						},
					},
				submitHandler:function (){
						$.ajax({
						url : '/index.php/project/add_project',
						type : 'POST',
						data:{
							name:$("#name").val(),
							status:$("input[name='status']:checked").val(),
						},
						dataType:'json',
						beforeSend: function(){
							$.jBox.tip("正在提交，请稍候", 'loading');		
						},
						success :function (data){
								$.each(data, function(i,n){
				  					if(n.status == '0'){
				  						$.jBox.tip(n.message, 'error')
				  					}
				  					if(n.status == '1'){
				  						var submit = function (v, h, f) {
										    if (v == true)
										    { 
										    	window.document.location.href='/index.php/project/add_project';
										    }
										    else
										    { 
										    	window.document.location.href='/index.php/project/add_project_info/'+n.insert_id;
										    }
										    return true;
										};
										$.jBox.closeTip();
										// 自定义按钮
										$.jBox.confirm(n.message, "提示", submit, { buttons: { '继续添加': true, '添加项目详细信息': false} });
				  					}
						  		 });
							},
						error : function (){
								$.jBox.tip('请求错误', 'error')
							}
					})
					return false;
				}		
			});

})
